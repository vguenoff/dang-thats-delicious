extends layout

include mixins/_storeCard

block content
  //- h1 You are on page #{title}
  .inner
    h2= title
    //- pre= h.dump(stores)
    .stores
      each store in stores
        +storeCard(store)
